[project]
name = "vllm-server"
version = "0.1.0"
description = "Qwen3.5-2B 本地 vLLM 推理服务"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "vllm>=0.19.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.10",
    "ty>=0.0.29",
]
